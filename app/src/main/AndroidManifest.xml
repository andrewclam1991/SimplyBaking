<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.andrewclam.bakingapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".StepListActivity"
            android:label="@string/title_step_list"
            android:launchMode="singleTop"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.andrewclam.bakingapp.MainActivity" />
        </activity>
        <activity
            android:name=".SimpleStepDetailActivity"
            android:label="@string/title_step_detail"
            android:launchMode="singleTop"
            android:parentActivityName=".StepListActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.andrewclam.bakingapp.StepListActivity" />
        </activity>

        <!-- TODO [ExoPlayer Media Playback ] Step 10 - Register receiver for MediaButton actions -->
        <receiver android:name=".StepDetailFragment$MediaReceiver">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".StepDetailActivity"
            android:label="@string/title_activity_step_detail__phone"
            android:launchMode="singleTop"
            android:parentActivityName=".StepListActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.andrewclam.bakingapp.StepListActivity" />
        </activity>

        <!-- TODO [Widget] Step 1 - Register the widget update receiver with the provider -->
        <receiver android:name=".SimplyBakingWidgetProvider">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/simply_baking_widget_info" />
        </receiver>

        <!-- TODO [Widget] Step 2 - Register the widget remote view service -->
        <service
            android:name=".services.SimplyBakingWidgetRemoteViewService"
            android:permission="android.permission.BIND_REMOTEVIEWS"
            android:exported="false"/>

        <service
            android:name=".services.SimplyBakingWidgetIntentService"
            android:exported="false" />
    </application>

</manifest>